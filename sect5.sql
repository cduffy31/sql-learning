/* SELECT store_id, count(store_id) from customer
group by store_id */

/* SELECT rating, count(rating) from film
group by rating */

/* SELECT rating, count(rating) from film
where rating != 'R'
group by rating */

/* SELECT customer_id, sum(amount) as am from payment 
group by customer_id
order by am desc
limit 10 */

